plugins {
    id 'cpp-library'
}

library {
    linkage = [Linkage.STATIC]

    source.from project.fileTree(dir: '.', include: '**/*.cpp')
    publicHeaders.from project.file('.')

    dependencies {
        api project(':list')
    }
}
